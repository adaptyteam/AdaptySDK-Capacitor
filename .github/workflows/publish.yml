name: Publish to npm

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'npm dist-tag to publish under'
        required: true
        default: 'latest'
        type: choice
        options:
          - 'latest'
          - 'beta'
      dry-run:
        description: 'Perform a dry run (no actual publish)'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

permissions:
  id-token: write  # Required for OIDC trusted publishing
  contents: read

jobs:
  guard:
    runs-on: ubuntu-slim
    name: Verify a real publish is from a v* git tag
    steps:
      - name: Enforce v* tag for real publishes (dry runs are exempt)
        env:
          DRY_RUN: ${{ inputs.dry-run }}
          REF_TYPE: ${{ github.ref_type }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          if [ "$DRY_RUN" = "true" ]; then
            echo "Dry run — skipping the v* tag guard (any ref is allowed)."
            exit 0
          fi
          if [ "$REF_TYPE" != "tag" ]; then
            echo "::error::A real publish must be dispatched from a git tag, but ref_type is '$REF_TYPE' (ref_name: '$REF_NAME')."
            exit 1
          fi
          case "$REF_NAME" in
            v*) echo "OK: real publish from tag '$REF_NAME'." ;;
            *)
              echo "::error::Tag '$REF_NAME' does not match the required 'v*' pattern."
              exit 1
              ;;
          esac

  validation:
    name: Run validation checks
    needs: guard
    uses: ./.github/workflows/pr-validation.yml

  publish:
    runs-on: ubuntu-slim
    environment: publish
    name: Publish package to npm
    needs: [guard, validation]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: |
          corepack enable || {
            echo "Corepack not available, falling back to npm install"
            npm install -g yarn@1.22.19
          }

      - name: Prepare Yarn
        run: |
          if command -v corepack > /dev/null 2>&1; then
            corepack prepare yarn@1.22.19 --activate
          fi

      - name: Cache node_modules
        uses: actions/cache@v4
        id: cache-node-modules
        with:
          path: node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            node-modules-${{ runner.os }}-

      - name: Install dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: yarn install --frozen-lockfile --prefer-offline

      - name: Generate version file
        run: yarn update-versions

      - name: Generate documentation (verification only)
        run: yarn doc-gen

      - name: Build package
        run: yarn build

      - name: Verify build outputs
        run: |
          echo "Checking dist directory..."
          ls -la dist/
          echo "Checking package files..."
          npm pack --dry-run

      - name: Publish to npm (dry-run)
        if: ${{ inputs.dry-run == 'true' }}
        env:
          NPM_DIST_TAG: ${{ inputs.tag }}
        run: npm publish --provenance --tag "$NPM_DIST_TAG" --dry-run --ignore-scripts

      - name: Publish to npm
        if: ${{ inputs.dry-run != 'true' }}
        env:
          NPM_DIST_TAG: ${{ inputs.tag }}
        run: npm publish --provenance --tag "$NPM_DIST_TAG" --ignore-scripts

      - name: Output published version
        if: ${{ inputs.dry-run != 'true' }}
        env:
          NPM_DIST_TAG: ${{ inputs.tag }}
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          echo "✅ Successfully published @adapty/capacitor@${PACKAGE_VERSION} (tag: ${NPM_DIST_TAG})"
          echo "📦 Package URL: https://www.npmjs.com/package/@adapty/capacitor/v/${PACKAGE_VERSION}"

